langcode: en
status: true
dependencies:
  module:
    - otp_field
title: 'Delete expired OTP secrets.'
id: otp_field_cron
weight: 0
module: otp_field
callback: otp_field_cron
scheduler:
  id: simple
  configuration:
    rules:
      - '0+@ * * * *'
launcher:
  id: serial
  configuration:
    timeouts:
      lock_timeout: 3600
    launcher:
      thread: 0
logger:
  id: database
  configuration:
    method: '3'
    expire: 1209600
    retain: 1000
