password_reset.reset_password:
  path: '/password/reset'
  defaults:
    _controller: '\Drupal\password_reset\Controller\PasswordResetController::resetPassword'
    _title: 'Reset User Password'
  requirements:
    _permission: 'access content'
